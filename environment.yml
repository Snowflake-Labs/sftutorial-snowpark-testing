# Snowpark environment file
# https://docs.snowflake.com/en/developer-guide/snowpark/python/setup.html

name: snowpark-testing
channels:
  - snowflake
dependencies:
  - python=3.9
  - pip
  - pip:
      - "-r requirements.txt"
      - "--editable ."
